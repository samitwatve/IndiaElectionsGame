// utils.js - Utility/helper functions for India Elections Game

// Add utility/helper functions here as needed.

export function clamp(val, min, max) {
  return Math.max(min, Math.min(max, val));
}

// Add more helpers as needed...
